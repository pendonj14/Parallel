# Distributed Order Processing

Lab activity for CS323 — building a master-worker system using MPI.

## Setup
```bash
pip install mpi4py
mpirun -np 4 python mpi_test.py
```

Each process should print a unique rank to confirm MPI is working.